---
  apiVersion: v1
  kind: PersistentVolumeClaim
  metadata:
    name: media-volume
  spec:
    storageClassName: manual
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 3Ti